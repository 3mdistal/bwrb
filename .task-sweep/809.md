# Task 809 ledger

- id: CONFIG-809
- source: GitHub issue #809
- source anchor: https://github.com/3mdistal/bwrb/issues/809
- title: Expose schema-supported date settings through `bwrb config`
- repo/workspace: `3mdistal/bwrb` in isolated worktree `issue-809-config`
- base branch/commit: `origin/main` at `cc04b6ed4fd0b38378424d01cd6086e419249894`
- problem: `ConfigSchema` accepts `date_format`, `date_granularity`, and `calendars`, while the command's separate allowlist rejects them.
- desired outcome: add safe, validated command support for scalar date settings; keep guided calendar object authoring in #790.
- acceptance checks: text and JSON list parity; JSON and interactive edit parity; valid persistence; invalid values do not write; command/documentation/agent-skill parity; no unsafe config keys exposed accidentally.
- manual successful user story: As a vault user, I can inspect and update `date_format` and `date_granularity` with `bwrb config`, receive stable text/JSON output, and get a validation error without a file change for malformed values.
- likely files/areas: `src/commands/config.ts`, `tests/ts/commands/config.test.ts`, canonical config/schema reference, `docs/skill/SKILL.md`.
- dependencies/blockers: #790 owns guided calendar-object authoring; no blocker for scalar date settings.
- parallel safety: independent in isolated worktree; docs overlap with other follow-up lanes must be resolved by parent before merge.
- status: draft PR open; tester failure fixed and awaiting independent retest/CI babysit
- working memory: this ledger and `.task-sweep/logs/809-*`
- child thread/agent: `/root/config_809`
- branch/PR: `codex/809-config-date-settings`; https://github.com/3mdistal/bwrb/pull/816 (draft)
- verification: initial focused command tests 38/38 source and dist plus config PTY 9/9; fix-loop source matrix 174/174 and dist config 39/39; config PTY remains 9/9; full non-PTY 2,986 passed and 3 skipped; build, verify:pack, typecheck, lint, knip, schema:check, docs:lint, docs:doctor, docs:check, docs build, and diff check passed; disposable built-CLI workflow now writes canonical ISO from configured `$TODAY`, preserves month partials, and audits clean
- Fable planning: `.task-sweep/logs/809-fable-plan-{prompt,output,metadata}-20260710-075132.md`; exact model returned a complete response with `finish_reason: stop`
- Fable review: original config review/follow-up under `.task-sweep/logs/809-fable-review-*`; tester-driven fix review under `.task-sweep/logs/809-fix-*` caught and fixed nondeterministic coverage, strict format precedence, and low-year ISO padding; final exact-model review `.task-sweep/logs/809-fix-review-final2-{prompt,output,metadata}-20260710-085259.md` returned `NO BLOCKERS` with `finish_reason: stop`
- planning decision: expose `date_format` and `date_granularity`; leave `calendars` and mention-tuning keys explicitly unexposed, with a parity test forcing future keys to be classified.
- tester/fix loop: independent tester reproduced `DD/MM/YYYY` `$TODAY` output failing immediate audit on an ambiguous calendar day. The fix parses explicitly configured patterns before canonical ISO storage, makes explicit non-ISO format precedence strict, retains canonical ISO/permitted partial compatibility, and covers escaped literals, invalid components, scalar/list parity, midnight-safe end-to-end behavior, and years 0001-0099.
- handoff: fresh tester should use the built CLI in a disposable vault to verify default and explicit text/JSON list output; valid JSON and interactive edits; missing/duplicated date-format tokens; invalid granularity with byte-identical schema; `calendars` rejection; and one downstream `$TODAY`/partial-date workflow under the edited settings
