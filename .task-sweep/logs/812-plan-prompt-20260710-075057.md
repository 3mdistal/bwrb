You are advising on Bowerbird issue #812 before implementation.

Task:
The canonical `list --body` filter and `list --body --matches` report promise Markdown-body search but the shared implementation scans the complete Markdown file, including YAML frontmatter. Make every shared `--body` consumer exclude top-of-file YAML frontmatter. Preserve literal/regex, case sensitivity, context, original file line numbers, path/type/where filters, limits, the Node fallback, and the hidden compatibility `search` command. Update tests and current docs/help. Do not broaden into unrelated search behavior.

Repo facts:
- Repo: 3mdistal/bwrb at fresh `origin/main` commit cc04b6ed4fd0b38378424d01cd6086e419249894.
- Node 22 CI; pnpm 10.11.0; TypeScript ESM.
- Canonical user docs live in `docs-site/src/content/docs/`; automation guidance in `docs/skill/SKILL.md`.
- Full CI parity: build, verify:pack, typecheck, lint, knip, non-PTY tests. Relevant extra gates: docs checks/build and PTY tests where needed.

Read-only repo context:
- `src/lib/content-search.ts::searchContent` discovers managed Markdown files then selects `runRipgrep` or `runNodeFallbackSearch`. Both currently scan each entire file. `runRipgrep` parses JSON match/context lines; fallback splits the entire content and checks every line. `ContentMatch` exposes original 1-indexed file line numbers.
- `src/lib/targeting.ts::resolveTargets` delegates all `--body` filtering to `searchContent` with literal, case-insensitive, zero-context options. This is used by list rows, bulk, audit, delete, edit, recent, open, and dashboards.
- `src/commands/search.ts::handleContentSearch` also delegates detailed `--body` matches to `searchContent`; canonical `list --body ... --matches` dispatches into this shared handler. The hidden compatibility `search ... --body` calls the same path.
- `src/lib/frontmatter.ts::parseNote` uses `gray-matter`, returning `{ frontmatter, body, raw }`, but the search result contract must keep original file line numbers.
- Current unit tests in `tests/ts/lib/content-search.test.ts` cover ripgrep, fallback indirectly, literal/case/regex/context/limit. Some command tests currently search `status`, which only exists in frontmatter and must be rewritten.
- Current docs deliberately disclose the bug with phrases such as “frontmatter included today” and link issue #812; those disclosures must be replaced by the shipped body-only contract.

Give a concise implementation plan covering:
- safest shared seam and frontmatter-boundary strategy,
- how to preserve original line numbers and keep context from leaking frontmatter,
- parity between ripgrep and Node fallback,
- regression tests for frontmatter-only false positives plus literal/case/regex/context and list/search compatibility,
- docs/help updates,
- edge cases and release risks.

You have no tools. Do not claim to inspect anything outside this packet. Treat this as planning advice only.
