You are advising on a repository task before implementation.

Task:
Fix https://github.com/3mdistal/bwrb/issues/809. Bowerbird's `ConfigSchema` accepts `date_format` (optional string), `date_granularity` (`day | month | year`), and `calendars` (record of validated calendar definitions), but `src/commands/config.ts` uses a separate `CONFIG_OPTIONS` allowlist and rejects all three for both `config list` and `config edit`. The issue explicitly allows calendar-object authoring to remain in separate issue #790, which asks for a guided `bwrb schema new calendar` flow.

Desired outcome:
- Expose the smallest safe date-setting surface through `bwrb config`.
- Preserve text and JSON output contracts.
- Validate values and avoid writing the schema on failure.
- Test allowlist/parity so future schema growth does not silently expose unsafe settings.
- Update canonical docs and `docs/skill/SKILL.md`.
- Do not implement guided calendar authoring or unrelated mention-tuning config.

Repo facts:
- Bowerbird is a TypeScript/Commander CLI.
- Base is fresh `origin/main` commit `cc04b6ed4fd0b38378424d01cd6086e419249894`.
- `pnpm@10.11.0`; CI uses Node 22.
- Full gate: build, verify:pack, typecheck, lint, knip, non-PTY tests. Schema/docs checks also exist.
- Canonical user docs are under `docs-site/src/content/docs/`; agent guidance is `docs/skill/SKILL.md`.

Read-only repo context:
- `CONFIG_OPTIONS` currently has ten entries: `link_format`, `editor`, `visual`, `open_with`, `obsidian_vault`, `default_dashboard`, `excluded_directories`, `mention_exclude_types`, `mention_exclude_paths`, and `mention_link_once`.
- The same `CONFIG_OPTIONS` drives specific/all listing, interactive selection, and JSON/interactive edits.
- `getConfigValue` returns effective defaults through a switch.
- JSON edit parses `--json`, runs `validateConfigValue`, normalizes selected arrays, assigns to the raw schema, then `writeValidatedSchema` runs `BwrbSchema.parse`, `resolveSchema`, and writes.
- `date_format` is used by date expansion/templates and defaults to `YYYY-MM-DD`; current schema validation accepts any string. Documentation describes patterns using `YYYY`, `MM`, and `DD`, with common formats `YYYY-MM-DD`, `MM/DD/YYYY`, `DD/MM/YYYY`, and `DD-MM-YYYY`.
- `date_granularity` defaults to `day`; its schema enum is `day | month | year`.
- `calendars` is a nested object with eras/months/hours and Zod super-refinement. Separate #790 requests a guided authoring flow.
- The config docs and skill currently state date settings are schema-only and link #809.
- Existing command tests cover list all/specific, JSON success/error, persistence, enum validation, and no-write behavior for a schema-level validation error.

Give a concise implementation plan:
- root-cause and minimal file changes,
- a recommendation on exposing `calendars` here versus leaving it to #790,
- robust value validation and default handling,
- a way to parity-test the explicit command allowlist without treating every future schema key as safe,
- focused and full tests plus real CLI scenarios,
- docs edits,
- risks and conflict cautions.

You have no tools. Do not claim to inspect anything outside this packet. Do not propose mutations beyond the stated task. Treat this as planning advice for another agent.
