NO BLOCKERS

All six prior risks are resolved in the final diff.

**Risk-by-risk verification**

1. **Identity precedence parity** — Resolved. Interactive pattern path (`src/commands/new/interactive.ts:~182-192`) uses `content.frontmatter.name ?? nameTransformed?.original ?? filename`; JSON path (`src/commands/new/json-mode.ts:87-91`) backfills only when `resolvedFrontmatter.name === undefined`, preserving explicit/template-defaulted names identically. Both are pinned by tests: `tests/ts/commands/json-io.test.ts` ("preserves an explicit identity when a pattern determines the filename", asserts `name: Canonical Identity`, path `Ideas/Pattern Path.md`) and `tests/ts/commands/new.pty.test.ts` ("preserves a template-defaulted identity when a pattern chooses the path", asserts `name: Stable Identity`).

2. **Explicit ancestor-directory precedence coverage** — Resolved. `tests/ts/commands/new.test.ts` ("inherits an explicit ancestor output_dir before computing a hierarchy") creates `entry extends collection` (only ancestor declares `output_dir: 'Library'`) and asserts `Library/Inherited Directory.md`.

3. **Resolver totality proven directly** — Resolved via the second permitted route. `tests/ts/lib/schema.test.ts` ("getOutputDir total resolution contract") covers explicit (`'Library'`), inherited (`'Library'`), computed (`'Events'`), and unknown (`'mysteries'`) cases. This justifies removing the JSON-mode error branch from `src/commands/new/write-plan.ts:resolveOutputDir` — the unreachable failure path is now backed by a proven total contract rather than dead defensive code.

4. **Slash-notation + computed fallback, owned placement, fork assessment** — Resolved. `new.test.ts` covers computed hierarchy via slash notation (`topic/entry` → `Topics/Entries/Computed Entry.md`) and canonical slash resolution (`objective/task` → `Objectives/Tasks/...`). `tests/ts/commands/new-ownership.test.ts:83-118` removes `output_dir` from `track` and asserts owned placement (`Albums/Best Album/songs/...`) bypasses the pooled resolver, with an explanatory comment. Forks are correctly untouched: the diff modifies only `write-plan.ts`, which forks (direct sibling-path implementation) do not consume, and no fork code changes appear.

5. **Docs colon** — Resolved. In `docs-site/src/content/docs/reference/commands/new.md:145-146`, the new normalization paragraph is inserted *after* the closing JSON code fence, so the colon-introduced example (line ~142) remains intact.

6. **Non-PTY field-order pin** — Resolved. `new.test.ts` computed-hierarchy test asserts `content.toMatch(/^type: topic\/entry\nid: [^\n]+\nname: Computed Entry$/m)`, pinning `type → id → name` order without PTY dependence. The PTY regex pin (`new.pty.test.ts:~150`) is now a redundancy, not the sole guard.

**Docs vs. implementation**: README, `reference/schema.md`, `validation-and-audit.md`, `quick-start.md`, `system-frontmatter.md`, and `SKILL.md` all now describe `output_dir` as optional with explicit-type → explicit-ancestor → computed-hierarchy precedence and dual-mode `name` persistence — consistent with `getOutputDir` usage in `write-plan.ts` and the name writes in both creation modes. The quick-start frontmatter example places `name` last, matching the pinned order.

**Non-blocking suggestions**

- `src/commands/new/json-mode.ts:271-277`: the error string `"Missing or invalid 'name' field"` fires for a *supplied but invalid* name (e.g., `{"name":42}` in the new test). Consider a distinct "invalid" message for supplied non-string values to aid callers.
- `src/commands/new/interactive.ts:209, 222` (non-pattern branches): `name: prompted` unconditionally overwrites any template-defaulted `frontmatter.name`. This is defensible (the prompt is explicit input, so explicit > default holds in both modes), but a one-line comment noting the intentional overwrite would prevent a future "parity" regression report.
- `tests/ts/commands/new.test.ts` computed-hierarchy test edits `schema.json` by direct write; a follow-up could route through a fixture helper for consistency with other schema-mutation tests.

Ready to exit the draft readiness gate.