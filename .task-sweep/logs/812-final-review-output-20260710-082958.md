NO BLOCKERS

**Verified against the diff:**

1. **Body-only semantics** — `restrictMatchesToMarkdownBody` drops matches with `line - 1 < bodyStartIndex` and rebuilds context from the original file, clamped at `bodyStartIndex`, so a body match adjacent to the closing `---` cannot display YAML lines. Applied uniformly after `searchImpl` resolves, so ripgrep and the Node fallback share identical restriction semantics (confirmed by the fallback test's `status:` empty-result assertion).

2. **Line numbers and CRLF** — original `match.line` values are preserved; text is renormalized from `content.split(/\r?\n/)` (CR-stripped), so CRLF match/context lines are clean. The trailing-newline `pop()` correctly avoids a phantom EOF context line. `findMarkdownBodyStartIndex` handles BOM and delimiter trailing whitespace, and returns 0 (whole file searchable) for incomplete blocks — safe-open rather than safe-hide. End-to-end CRLF test asserts `line: 5` with body-only context.

3. **Laziness** — `loadSearchableFileSources([...rawResults.keys()], ...)` reads only raw-matched files, not the candidate set; zero-match queries read nothing extra.

4. **Limit interaction** — `runRipgrep` is not passed `limit`, so truncation occurs after body restriction; the `AAA Limit Decoy.md` test (sorts before `Boundary Match.md`, `limit: 1`) asserts the surviving path is the real body match, and evidence shows it passing.

5. **Docs/help coherence** — help text, README, all six command reference pages, both targeting docs, SKILL.md, and the changelog consistently state frontmatter exclusion, original line numbers, and `--where` for frontmatter fields; the old "#812 tracked" framing is fully removed. Scope stays within #812.

**Non-blocking observations:**
- `readFile`/`join` usage in the new `content-search.ts` code shows no import additions in the diff; presumably already imported for the Node fallback — typecheck/build passing confirms.
- A body-only file whose first line is a `---` thematic break followed by a later `---` would be misread as frontmatter; this is the standard heuristic and pre-existing to frontmatter parsing generally, not a regression.
- The `list.test.ts` fallback test hardcodes a POSIX-style `PATH`; pre-existing pattern, unchanged in portability.

Evidence (full suites, PTY, focused matrix, build/lint/docs gates, Node 22 disposable-vault story) is consistent with the diff. Merge-ready.