You are advising on a Bowerbird CLI repository task before implementation.

Task:
- GitHub issue #810: synchronize root commands, subcommands, and option candidates with the registered CLI.
- Root completion must include every visible command and continue excluding hidden compatibility commands `open` and `search`.
- Schema completion must include visible `types`, `fields`, and `discover` alongside existing commands.
- Template completion must remove nonexistent `show`; `template list <type> <name>` is the current detail surface.
- Recent option completion must include shipped `--open`/`-o`, `--app`, `--save-as`, and `--force` flags.
- Add stable contract tests against registered/built CLI help where appropriate and update canonical completion docs.
- Keep the fix narrow. Do not change hidden-command policy or unrelated behavior.

Repo facts:
- Repo: 3mdistal/bwrb; default branch `main`.
- Base: `cc04b6ed4fd0b38378424d01cd6086e419249894`.
- Runtime: Node 22 in CI, pnpm 10.11.0.
- Full CI parity: build, verify:pack, typecheck, lint, knip, non-PTY tests.
- Canonical docs: `docs-site/src/content/docs/`.
- Completion implementation: `src/lib/completion.ts`; registration: `src/index.ts`.

Read-only repo context:
- Root built help visibly registers: new, edit, delete, list, recent, schema, audit, bulk, template, dashboard, init, config, completion.
- `src/index.ts` registers hidden `open` and `search` with `{ hidden: true }` and says they stay out of root help/completion.
- Current root completion candidates are: new, edit, list, recent, audit, bulk, schema, template, dashboard, delete, completion, config. It omits `init`.
- Built `schema --help` commands: types, fields, validate, discover, new, edit, delete, list, diff, migrate, history. Completion currently has only list, new, edit, delete, validate, diff, migrate, history.
- Built `template --help` commands: list, validate, new, edit, delete. Completion currently also advertises nonexistent `show` and contains positional logic/tests for it.
- Built `recent --help` options include selectors, limit/output, `-o, --open`, `--app`, `--save-as`, and `--force`. Completion lacks those four workflows and `-o`.
- Existing tests mostly assert selected inclusions, allowing the tables to drift. The suite already builds before command tests, and `tests/ts/lib/completion.test.ts` directly exercises completion functions.
- Canonical shell-completion docs explicitly describe the current `init` omission and link issue #810; installation docs repeat it.

Give a concise implementation plan covering:
- root-cause and minimal file changes,
- a stable test strategy that catches future help/completion drift without teaching hidden compatibility commands,
- positional-completion cleanup required by removing `template show`,
- focused real CLI checks and full gates,
- documentation edits,
- risks and edge cases.

You have no tools. Do not claim to inspect anything outside this packet. Do not propose mutations beyond the stated issue. Treat this as planning advice for another agent.
