NO BLOCKERS

**Contract check (diff-only):**

- **Root completions:** `COMMANDS` now includes `init` (and reorders to match help); new test asserts exact equality against built `--help` commands (minus `help`) and explicitly excludes `open`/`search`. ✓
- **Schema/template subcommands:** `SUBCOMMANDS` updated (`show` removed from template; `types`/`fields`/`discover` added to schema); parameterized exact-set tests against built subcommand help. ✓
- **Template detail-name surface:** handler moves `list` into the `[type] [name]` group, drops `show`; new test verifies `template list task <TAB>` completes template names. ✓
- **Recent options:** `--open`, `-o`, `--app`, `--save-as`, `--force` added; new test is bidirectional (set equality of completions vs. built help flags minus `-h`, plus `--vault`/`-v`/`--non-interactive`) with a duplicate guard, as the prior review requested. ✓
- **Docs:** README, installation, shell-completion, and completion reference pages now match the updated runtime surface (command lists and subcommand tables align with `COMMANDS`/`SUBCOMMANDS`); stale #810 caveats and roadmap entries removed consistently. ✓

**Non-blocking suggestions:**

1. The root and schema/template contract tests use order-sensitive `toEqual`. If help reorders commands cosmetically, the tests will fail even though the candidate *set* is intact. Consider sorting both sides (or documenting that ordering is intentionally part of the contract).
2. `extractHelpOptionFlags` relies on `  -`-prefixed lines and two-space signature splitting; a brief comment noting the help-format assumption would help future maintainers if help formatting changes.
3. The reference docs describe schema completions as "including `types`, `fields`, and `discover`" while shell-completion.md lists the full set — harmless, but aligning both to the full list would avoid ambiguity.

Ready to merge.