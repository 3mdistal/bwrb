NON-BLOCKING

**Verdict: No correctness or release blockers found in the embedded diff.** The code, tests, and docs are internally consistent with the issue contract:

- Root `COMMANDS` includes `init`, excludes `open`/`search` (src/lib/completion.ts:246–260), and doc tables match the new order exactly (README.md, docs-site completion.md, installation.md).
- `SUBCOMMANDS.schema` gains `types`/`fields`/`discover`; `template` drops `show` (src/lib/completion.ts:63–64).
- `template list` moved to the `[type] [name]` branch, preserving detail completion (src/lib/completion.ts:519–520, 543–544), with a new second-positional test (tests/ts/lib/completion.test.ts:543–560).
- `recent` options gain `--open`/`-o`/`--app`/`--save-as`/`--force` (src/lib/completion.ts:276), matching new doc tables.
- Stale issue #810 references removed from both roadmaps.

**Non-blocking suggestions:**

1. **Unverified helper import** — `extractHelpCommands` is imported from `../helpers/help` (tests/ts/commands/completion.test.ts:6) but not added in this diff. If it doesn't already exist, all three contract tests fail at import time. Confirm it's pre-existing.
2. **Order-sensitive contract tests** — `expect(completions).toEqual(helpCommands)` (tests/ts/commands/completion.test.ts:104, 113) requires the hardcoded `COMMANDS`/`SUBCOMMANDS` arrays to match help output order exactly. Any harmless reordering in help breaks CI. Consider sorting both sides; set membership is the real contract.
3. **One-directional recent options contract** — `expect.arrayContaining(helpOptions)` (tests/ts/commands/completion.test.ts:122) only asserts help ⊆ completions. Stale/nonexistent flags in `COMMAND_OPTIONS.recent` would pass silently — the exact drift class this PR fixes for `template show`. Consider asserting the reverse inclusion too (modulo `-h`).
4. **Dashboard subcommands untested against help** — the contract `it.each` covers only `schema`/`template`; `dashboard` remains drift-prone with no built-CLI check.
5. **Fragile option-line parsing** — `extractHelpOptionFlags` (tests/ts/commands/completion.test.ts:36–45) assumes single-line, two-space-indented signatures; wrapped help lines would silently drop flags, weakening the contract rather than failing it.
6. **Built-artifact dependency** — `runBuiltCliOutput` executes `CLI_PATH` directly; ensure CI builds before this test file runs, or these tests fail with confusing ENOENT-style errors.