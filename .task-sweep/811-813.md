# Task sweep: issues 811 and 813

## Normalized task

- id: `NEW-811-813`
- sources: GitHub issues [#811](https://github.com/3mdistal/bwrb/issues/811) and [#813](https://github.com/3mdistal/bwrb/issues/813)
- repo/workspace: `3mdistal/bwrb`; isolated worktree `issues-811-813-new`
- base branch/commit: `origin/main` at `cc04b6ed4fd0b38378424d01cd6086e419249894`
- branch: `codex/811-813-new-creation-parity`
- problem: ordinary creation rejects types whose valid output directory is computed, while interactive and JSON creation persist the built-in `name` differently.
- desired outcome: use the schema's canonical output-directory resolution for ordinary creation, preserving explicit and owned placement; persist a canonical built-in `name` for both interactive and JSON paths without confusing filename sanitization or filename-pattern transforms with note identity.
- acceptance checks: JSON and PTY interactive creation; computed and explicit directories; filename patterns/name transforms; templates, scaffolding, ownership, forks; field order/system fields; collisions and errors; canonical docs and agent skill.
- manual successful user story: as a Bowerbird user, I can create the same type interactively or through JSON without an explicit `output_dir`; both notes land in the schema-computed directory and persist the name I supplied even if the filename is safely transformed.
- likely areas: `src/commands/new/{write-plan,interactive,json-mode}.ts`, schema helpers, `tests/ts/commands/new*.test.ts`, canonical `new` reference, system-frontmatter product note, `docs/skill/SKILL.md`.
- dependencies/blockers: bundled because both defects share creation content/write planning; no external blocker known.
- parallel safety: must-serialize within this lane; independent tester owned by the parent orchestrator.
- status: locally verified; draft PR publication pending

## Evidence and verification

- workspace discovery: dirty source `main` preserved; isolated worktree created from the requested fresh base.
- repository policy: strict required checks are `Test`, `PTY Tests`, and `Vercel`; no required approving reviews; draft PR required by sweep.
- package/runtime: `pnpm@10.11.0`; Node 22 in CI.
- Fable planning: exact `anthropic/claude-fable-5` completion succeeded (`anthropic/claude-5-fable-20260609`, `finish_reason: stop`); artifacts: `.task-sweep/logs/811-813-plan-{prompt,output}-20260710-081444.md` and `.task-sweep/logs/811-813-plan-20260710-081444.log`.
- implementation: pooled creation now uses `getOutputDir` with the canonical resolved type name; interactive creation persists the raw prompted identity or the pre-normalization filename-pattern value; JSON derives a missing name from a resolving pattern while preserving explicit names and rejecting invalid supplied names.
- compatibility boundaries: owned placement still bypasses pooled placement even when the owned type omits `output_dir`; explicit type/ancestor directories still win through the shared schema resolver; filenames remain normalized separately. Native forks use `src/commands/new/fork.ts` and direct sibling paths rather than `write-plan.ts`; the unchanged 22-test fork suite passed. Instance scaffolding continues to use its child-type resolver and its full suite passed.
- docs: temporary #811/#813 caveats replaced with positive current contracts in README, canonical new/schema/Quick Start/validation docs, product system-frontmatter policy, and agent skill. Historical changelogs remain untouched.
- local gates: install/build passed. Final exact CI sequence passed: build, verify:pack, typecheck, lint, knip, and 2,981 non-PTY tests (3 skipped). Final focused matrix passed 115 non-PTY tests and 22 `new` PTY tests. Full PTY first run had four unrelated width assertions fail because local Node 26 printed a 115-character `DEP0205` warning; final unchanged run with `NODE_OPTIONS=--no-deprecation` passed all 94 suites (178 passed, 1 skipped, 0 failed). Schema parity, docs lint/doctor/check, clean-cache docs build (41 pages, no duplicate-ID warning), and `git diff --check` passed.
- Fable review: first frozen-diff call at `083538` returned empty content with `finish_reason: length` and was preserved as invalid evidence; same-prompt retry returned `NON-BLOCKING`. The orchestrator promoted its parity/safety coverage notes to required; all were fixed. Final full-diff follow-up used exact `anthropic/claude-fable-5`, returned `anthropic/claude-5-fable-20260609`, `finish_reason: stop`, verdict `NO BLOCKERS`, and explicitly verified all six risks resolved. Artifacts: `.task-sweep/logs/811-813-review-*083538*` and `.task-sweep/logs/811-813-final-review-*20260710-085735*`.
- PR/CI: pending.
- handoff: fresh tester scenarios will be provided to the parent orchestrator; this lane will not self-test as the independent tester.
